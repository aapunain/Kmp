---
inclusion: fileMatch
fileMatchPattern: '**/*.kt'
---

# Kotlin Multiplatform pitfalls

The only Kiro-specific thing here is *when* this loads: conditionally, whenever a
Kotlin file is in context, rather than on every turn. The content is the canonical
pitfalls log, inlined rather than summarised, so it cannot drift from the real one.

Add new entries to `docs/KMP_PITFALLS.md`, never here.

#[[file:docs/KMP_PITFALLS.md]]

<!--
Fallback if the inline reference above does not resolve: read docs/KMP_PITFALLS.md.
-->

See [docs/KMP_PITFALLS.md](../../docs/KMP_PITFALLS.md).
